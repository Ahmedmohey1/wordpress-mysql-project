# WordPress with MySQL Docker Compose Project

This project sets up a WordPress site with a MySQL database using Docker Compose.

## Prerequisites
- Docker
- Docker Compose

## Setup
1. Clone the repository:
   ```bash
   git clone https://github.com/<your-username>/wordpress-mysql-project.git